.DS_Store
.idea
.vscode
**tmp**
build-and-push.sh

# Optional npm install under webjs/ (unit tests need no deps by default)
node_modules/

# Local SQLite databases and WAL/SHM sidecars (never commit operator data)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Local worklogs / personal trackers (not committed)
*.local.md

# Local binaries (built by go build -o …)
/openfsd-web
/fsdweb
/fsd
/openfsd
/openfsd-migrate-to-rqlite

# User-fetched X-Plane Global Airports data and bulk conversion output.
# Tooling under cmd/, internal/xp12aptdat/, scripts/ is tracked.
# See docs/xplane-airport-data.md.
/xp12-aptdat/
/generated-apt/
apt.dat
apt.dat.zip
**/apt.dat
**/apt.dat.zip
cover.out

# Local client research downloads/extracts (never redistribute third-party binaries)
.research/
